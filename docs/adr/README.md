# Architecture Decision Records

One file per decision, named `NNN-slug.md` (e.g. `001-incremental-wav-writing.md`).

Each ADR should capture: the context/problem, the decision, and the consequences
(trade-offs accepted). Keep them short and append-only — supersede rather than
edit once a decision has shipped.
