# Audio recording
sounddevice>=0.5.0
soundfile>=0.13.0
numpy>=1.26.0

# Transcription - Whisper (default)
mlx-whisper>=0.4.0

# Transcription - Parakeet MLX (default engine in config.yaml)
parakeet-mlx>=0.2.0

# Speaker Diarization (optional)
# pyannote.audio>=3.1.0

# Transcription - IndicWhisper fallback (optional, non-MLX)
# transformers>=4.40.0
# torch>=2.2.0
# scipy>=1.12.0

# LLM providers — all three installed; configure which to use in config.yaml
google-genai>=1.0.0
openai>=1.0.0
anthropic>=0.40.0

# macOS menu bar
rumps>=0.4.0

# Config
pyyaml>=6.0

# CLI
click>=8.1.0

# MCP server (mcp_server.py)
mcp>=1.0.0

# Web front end (web/app.py)
fastapi>=0.110
uvicorn[standard]>=0.27
jinja2>=3.1
python-multipart>=0.0.9
markdown>=3.5
dateparser>=1.2

# Dev / tests
pytest>=8.0
